//
// Created by 24087 on 24-11-10.
//
#include <iostream>
#include <set>

struct Person {
    int l, r, idx;
};

int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
    std::cout.tie(nullptr);

    int t;
    std::cin >> t;
    while(t--) {
        int n, f;
        std::cin >> n >> f;
        std::set<Person> st;
    }
}